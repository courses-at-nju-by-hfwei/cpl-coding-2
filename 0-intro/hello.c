// stdio: standard input/output
#include <stdio.h>

// below is a comment
// main is a function: z = f(x, y) = x + y
// int: integer
// void: no parameters
int main() {
  // printf is also a function
  // printf: print + format (格式)
  // "xyz": string (字符串)
  printf("Hello, World!\n");
  // if no "return 0", the compiler will add it for you automatically
  return 0;
}
